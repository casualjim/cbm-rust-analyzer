#!/usr/bin/env bash

#MISE description="Clean all the generated files in the project"

echo "🚀 Cleaning generated artifacts..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

rm -f include/rust_analyzer_lsp.h
rm -rf target/prefix target/c_abi_smoke target/tmp/rust_analyzer_lsp.generated.h
cargo clean
