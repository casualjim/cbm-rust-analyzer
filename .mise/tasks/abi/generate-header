#!/usr/bin/env bash

#MISE description="Generate the Rust analyzer C ABI header"
#MISE sources=["src/capi.rs", "cbindgen.toml"]
#MISE outputs=["include/rust_analyzer_lsp.h"]

echo "🚀 Generating Rust analyzer C ABI header..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

set -euo pipefail

RUST_ANALYZER_GENERATE_HEADER=1 cargo build
